# Environment Configuration

# Database connection URL (SQLite by default)
DATABASE_URL="file:./dev.db"

# Application URL (used for server-side references)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
